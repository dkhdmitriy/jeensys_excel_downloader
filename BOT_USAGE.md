# Telegram-бот для анализа устройств

## Как получить токен бота

1. Откройте Telegram и найдите бота **@BotFather**
2. Отправьте команду `/newbot`
3. Следуйте инструкциям (придумайте имя и username)
4. Скопируйте полученный токен в переменную `TELEGRAM_BOT_TOKEN`

## Как получить свой Chat ID

1. Запустите бота:
   ```powershell
   python .\bot.py
   ```

2. В Telegram найдите своего бота и отправьте команду `/start`

3. В консоли будет выведен ваш Chat ID

4. Используйте этот ID для отправки команд боту

## Команды бота

- `/start` - Показать приветствие и справку
- `/report` - **Скачать последний Excel файл** и получить анализ устройств
- `/help` - Показать справку по командам

## Как работает команда /report

1. Бот запускает скрипт `main.py` для скачивания нового Excel файла с mon.jeensys.com
2. После успешного скачивания анализирует загруженный файл
3. Отправляет отчёт в Telegram (автоматически разбивает на части если нужно)

## Пример использования

```powershell
# Активируем окружение
.\.venv\Scripts\Activate.ps1

# Устанавливаем переменные окружения
$env:JEENSYS_LOGIN = 'your_login'
$env:JEENSYS_PASSWORD = 'your_password'
$env:TELEGRAM_BOT_TOKEN = 'your_bot_token'

# Запускаем бота
python .\bot.py
```

Теперь можно отправлять команды боту в Telegram!

## Формат отчёта

Бот отправляет отчёт в следующем формате:

```
==================================================
📊 ОТЧЁТ ПО УСТРОЙСТВАМ
==================================================

💰 LTC СЕГМЕНТ:
L7-10,16 (224шт-2276хэш)
L9-18,45 (248шт-4576хэш)
ИТОГ LTC 6852

₿ BTC СЕГМЕНТ:
WM-117,88 (86шт-10138хэш)
T21-192,60 (194шт-37365хэш)
S19-98,72 (1637шт-161613хэш)
S19 доп-101,18 (418шт-42291хэш)
S19 emcd-105,53 (10шт-1055хэш)
Среднее S19 - 99,25 (2065шт-204959хэш)
ИТОГ BTC 252462
```
