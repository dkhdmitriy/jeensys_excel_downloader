# 📋 Реализованные функции

## main.py - Основной скрипт анализа

### Функции Selenium (браузер автоматизация):
- `read_credentials()` - Чтение учётных данных из переменных окружения
- `build_driver()` - Инициализация Chrome драйвера с нужными настройками
- `perform_login()` - Авторизация на mon.jeensys.com
- `open_devices_page()` - Переход на страницу устройств
- `select_all_devices()` - Выделение всех устройств
- `ensure_settings_panel_open()` - Открытие панели настроек
- `start_excel_download()` - Инициирование скачивания Excel файла
- `export_devices_excel()` - Общая функция экспорта (объединяет выше перечисленные)

### Функции анализа Excel:
- `find_latest_excel()` - Поиск последнего загруженного Excel файла (исключает временные ~$ файлы)
- `parse_excel_devices()` - Парсинг Excel с применением фильтров по клиентам:
  - Применяет фильтр на каждого клиента отдельно
  - Использует точное совпадение названия (без подстрок)
  - Читает название из столбца I, хэшрейт из столбца O
  - Нормализует хэшрейт (делит на 1000)
  - Выводит статистику по фильтрам

### Функции форматирования и отчётности:
- `calculate_stats()` - Расчёт статистики (количество, общий и средний хэшрейт)
- `format_number()` - Форматирование чисел с запятой вместо точки
- `generate_report()` - Генерация и вывод отчёта в консоль:
  - **LTC сегмент**: L7, L9 с ИТОГОМ
  - **BTC сегмент**: WM, T21, S19, S19 доп, S19 emcd с общей статистикой по S19 и ИТОГОМ
- `analyze_and_report()` - Объединённая функция (поиск → парсинг → отчёт)
- `main()` - Главная функция (Selenium + анализ Excel)

## bot.py - Telegram бот

### Команды:
- `/start` - Приветствие с показом Chat ID
- `/help` - Справка по командам
- `/report` - **Скачивание нового Excel файла + анализ и отправка отчёта**

### Функции:
- `start()` - Обработчик /start команды
- `help_command()` - Обработчик /help команды
- `format_report()` - Форматирование отчёта для Telegram (с эмодзи)
- `report()` - Обработчик /report команды:
  - **Запускает main.py в headless режиме** для скачивания Excel файла
  - Ждёт завершения скачивания (таймаут 5 минут)
  - Парсит устройства из загруженного файла
  - Отправляет отчёт в Telegram
  - Автоматически разбивает большие сообщения (лимит 4096 символов)
  - Включает логирование всех этапов
- `main()` - Запуск бота с обработчиками команд

## Особенности реализации

✅ **Точное совпадение клиентов** - Использование `==` вместо `in` для избежания пересечений (S19 не захватывает S19 XP)

✅ **Фильтрация по клиентам** - Для каждого клиента применяется отдельный фильтр перед анализом

✅ **Нормализация хэшрейта** - Значения из Excel делятся на 1000 для корректного отображения

✅ **Исключение временных файлов** - Временные файлы Excel (~$...) исключаются из поиска

✅ **Форматирование чисел** - Использование запятой вместо точки в соответствии с русской локализацией

✅ **Логирование** - В боте реализовано логирование всех операций

✅ **Обработка ошибок** - Корректная обработка исключений с информативными сообщениями

✅ **Telegram интеграция** - Автоматический разбор больших отчётов, поддержка Markdown

## Файлы проекта

```
jeensys_parser/
├── main.py              # Основной скрипт (Selenium + анализ Excel)
├── bot.py              # Telegram бот
├── requirements.txt    # Зависимости проекта
├── README.md           # Основная документация
├── BOT_USAGE.md        # Документация по использованию бота
├── .env.example        # Пример файла с переменными окружения
└── .github/
    └── copilot-instructions.md  # Инструкции для AI ассистента
```

## Переменные окружения

Необходимые для работы переменные окружения (в файле `.env` или shell):

```
JEENSYS_LOGIN=your_login
JEENSYS_PASSWORD=your_password
TELEGRAM_BOT_TOKEN=your_bot_token
```
