version: 1
meta:
  environment: docker
  toolchain:
    name: docker
    version: latest
build:
  dockerfile: Dockerfile
run:
  command:
    - python
    - bot.py
  persistenceMount: /data
  containerPort: 80
  image: jeensys-bot:latest
