---
version: null
meta:
  environment: docker
  toolchain:
    name: docker
    version: latest
build:
  dockerfile: DockerFile
run:
  command:
  - python
  - bot.py
  persistenceMount: /data
  containerPort: 80
  image: jeensys-bot:latest
